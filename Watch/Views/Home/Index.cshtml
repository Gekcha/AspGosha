@model HomeViewModel

<h1>Лучшие автомобили</h1>
<div class="row mt-5 mb-2">
    @{
        foreach (Watch watch in Model.favWatches)
        {
            @Html.Partial("AllWatches", watch);
        }
    }

</div>