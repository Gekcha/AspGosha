        <h1>Все автомобили</h1>
        <h3>@Model.currCategory</h3>
<div class="row mt-5 mb-2">
    @{
        foreach (Watch watch in Model.allWatches)
        {
            @Html.Partial("AllWatches", watch);
        }
    }

</div>